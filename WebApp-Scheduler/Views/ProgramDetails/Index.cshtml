@model IEnumerable<WebApp_Scheduler.Models.ProgramDetails>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th></th>
        <th>
            @Html.DisplayNameFor(model => model.ProgramName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ProgramStartDate)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ProgramEndDate)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TotalTeachingHoursOfDay)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td> @Html.ActionLink("All-Courses", "Index", "Courses",new { ProgramId = item.Id }, new { @class = "btn btn-default" }) </td>
            <td>
                @Html.DisplayFor(modelItem => item.ProgramName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ProgramStartDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ProgramEndDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TotalTeachingHoursOfDay)
            </td>
            <td>

                @Html.ActionLink("Edit", "Edit", new { id = item.Id }) |
                @Html.ActionLink("Details", "Details", new { id = item.Id }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.Id })
            </td>
        </tr>
    }

</table>
